//ex.1
//este algoritmo

export function calcAlgoritmo(num: number): number {
    let res: number = 1;
    let x: number;
    for (x = num; x >= 1; x--) {
        res = res * x;
    }
    return res
}

//ex.2

